# 冒泡排序中的指针用法（数组传参 + 隐式解引用）🫧

> 目标：通过“冒泡排序”这段代码，彻底理解 **数组传参为什么要用 `int *arr`**、**为什么没手写 `*` 也能访问值**、以及 **`arr[j]` 和 `*(arr + j)` 的本质关系**。



## 1. 原始代码（数组下标写法）

```cpp
#include <iostream>
using namespace std;

void bubbleSort(int *arr, int len) {
    for (int i = 0; i < len - 1; i++) {
        for (int j = 0; j < len - 1 - i; j++) {
            if (arr[j] > arr[j + 1]) {
                int temp = arr[j];
                arr[j] = arr[j + 1];
                arr[j + 1] = temp;
            }
        }
    }
}

void printArray(int *arr, int len) {
    for (int i = 0; i < len; i++) {
        cout << arr[i] << " ";
    }
}

int main() {
    int arr[10] = {4, 6, 3, 8, 2, 9, 0, 1, 5, 7};
    int len = sizeof(arr) / sizeof(arr[0]);

    bubbleSort(arr, len);
    printArray(arr, len);

    return 0;
}
````

---

## 2. 这段代码里“指针”到底出现在哪？🎯

虽然你写的是 `arr[j]` 这种“数组形式”，但在函数里它本质上就是 **指针访问**。

### 关键点

函数参数写成了：

```cpp
void bubbleSort(int *arr, int len)
void printArray(int *arr, int len)
```

这里的 `arr` 不是数组本体，而是：

* 一个指针（`int*`）
* 指向数组首元素（`&arr[0]`）

也就是说，函数通过这个指针访问和修改原数组。

---

## 3. 为什么这里要写 `*` ？（`int *arr`）✅

### 写法含义

```cpp
int *arr
```

表示：

* `arr` 是一个变量
* 它的类型是“**指向 int 的指针**”
* 它里面存的是一个地址（这个地址指向 `int` 数据）

---

### 如果不写 `*` 会怎样？

如果写成：

```cpp
void bubbleSort(int arr, int len)
```

那 `arr` 就变成了一个普通整数（`int`），而不是地址。
这样就不能表示整个数组，也不能写 `arr[j]` 访问多个元素。

---

## 4. 数组传参时发生了什么（核心规则）📌

在 `main` 中：

```cpp
int arr[10] = {4, 6, 3, 8, 2, 9, 0, 1, 5, 7};
bubbleSort(arr, len);
```

这里的 `arr` 是数组名，但在传参时会发生一件事：

## 数组名会退化为首元素地址（`&arr[0]`）

也就是说，传给函数的其实不是整个数组，而是：

* 数组第一个元素的地址
* 类型正好是 `int*`

所以函数参数必须写成：

```cpp
int *arr
```

---

## 5. 为什么没手写 `*` 也能访问值？（你问的重点）🔥

因为：

## `arr[j]` 本身就已经包含了解引用操作

也就是说，`arr[j]` 在底层等价于：

```cpp
*(arr + j)
```

你没有显式写 `*`，是因为 `[]` 运算符已经帮你做了。

---

## 6. `arr[j]` 的底层拆解（一步一步看）🧠

假设 `arr` 是一个指针，指向数组首元素：

```cpp
int *arr;
```

当你写：

```cpp
arr[j]
```

底层会做两件事：

### 第一步：指针偏移

```cpp
arr + j
```

表示从首地址开始，向后移动 `j` 个 `int` 元素的位置。

> 注意：这里移动的是“元素个数”，不是字节数。
> 如果是 `int*`，每移动 1 次就是跳过 1 个 `int`（通常 4 字节）。

---

### 第二步：解引用取值

```cpp
*(arr + j)
```

表示取出这个位置上存放的数据。

---

### 所以结论就是

```cpp
arr[j] == *(arr + j)
```

这就是为什么你看起来没写 `*`，却仍然能访问到地址中的值。

---

## 7. 在冒泡排序中，这两种写法完全等价 ✨

### 写法 A：数组下标形式（常见）

```cpp
if (arr[j] > arr[j + 1]) {
    int temp = arr[j];
    arr[j] = arr[j + 1];
    arr[j + 1] = temp;
}
```

### 写法 B：纯指针形式（本质）

```cpp
if (*(arr + j) > *(arr + j + 1)) {
    int temp = *(arr + j);
    *(arr + j) = *(arr + j + 1);
    *(arr + j + 1) = temp;
}
```

两者功能完全一样，只是写法不同。

---

## 8. 纯指针版冒泡排序（推荐收藏）📘

这版代码把“底层本质”写出来了，更适合学习指针：

```cpp
#include <iostream>
using namespace std;

// 冒泡排序（纯指针写法）
void bubbleSort(int *arr, int len) {
    for (int i = 0; i < len - 1; i++) {
        for (int j = 0; j < len - 1 - i; j++) {
            if (*(arr + j) > *(arr + j + 1)) {
                int temp = *(arr + j);
                *(arr + j) = *(arr + j + 1);
                *(arr + j + 1) = temp;
            }
        }
    }
}

// 打印数组（纯指针写法）
void printArray(const int *arr, int len) {
    for (int i = 0; i < len; i++) {
        cout << *(arr + i) << " ";
    }
    cout << endl;
}

int main() {
    int arr[10] = {4, 6, 3, 8, 2, 9, 0, 1, 5, 7};
    int len = sizeof(arr) / sizeof(arr[0]);

    bubbleSort(arr, len);
    printArray(arr, len);

    return 0;
}
```

---

## 9. 为什么函数里能修改原数组？（不是副本吗？）🤔

能修改，是因为函数拿到的是“地址”，不是新拷贝的一份数组。

* `main` 里的数组在内存里有一块真实空间
* 函数里的 `arr` 指针指向这块空间
* 你在函数里改 `*(arr + j)`，本质是在改原来的内存

所以排序结束后，`main` 中的数组也已经变成有序了。

---

## 10. 为什么还要传 `len`？（这个细节很重要）⚠️

在 `main` 中你能写：

```cpp
int len = sizeof(arr) / sizeof(arr[0]);
```

因为这里的 `arr` 是完整数组（类型是 `int[10]`）。

---

但在函数里：

```cpp
void bubbleSort(int *arr, int len)
```

这里的 `arr` 已经是指针（`int*`）了，不再是完整数组。
如果在函数里写：

```cpp
sizeof(arr)
```

得到的是：

* 指针大小（通常 4 字节或 8 字节）
* **不是整个数组大小**

所以必须把长度 `len` 单独传进去 ✅

---

## 11. `*` 的两种含义（很容易混淆）🧩

同一个符号 `*`，在不同位置含义不同：

---

### ① 在声明中：表示“指针类型”

```cpp
int *p;
```

意思是：

* `p` 是一个指针
* 指向 `int`

---

### ② 在表达式中：表示“解引用”

```cpp
*p
```

意思是：

* 取出 `p` 指向地址里的值

---

### 对比理解

```cpp
int x = 10;
int *p = &x;  // 声明指针，p 保存 x 的地址

cout << p << endl;   // 输出地址
cout << *p << endl;  // 输出 10（解引用取值）
```

---

## 12. 这段代码中的指针知识总结（可直接放笔记）📝

### 1）数组传参会退化为指针

数组名在函数传参时会退化为首元素地址，因此函数通常写成 `int *arr` 来接收。

### 2）`arr[j]` 本质是指针运算

`arr[j]` 等价于 `*(arr + j)`，其中 `[]` 已经隐式完成了解引用。

### 3）函数通过指针操作原数组

函数拿到的是原数组首地址，所以在函数里修改元素，会直接影响 `main` 中的数组。

### 4）长度必须单独传递

函数参数中的 `arr` 是指针，不是完整数组，不能用 `sizeof(arr)` 计算数组长度，因此必须传入 `len`。

---

## 13. 一张图理解（数组 / 指针 / 下标关系）🌉

假设数组是：

```cpp
int arr[5] = {10, 20, 30, 40, 50};
```

内存示意（假设 `int` 占 4 字节）：

```text
arr（首地址）
  ↓
+------+------+------+------+------+
| 10   | 20   | 30   | 40   | 50   |
+------+------+------+------+------+
  ^      ^      ^      ^      ^
arr+0  arr+1  arr+2  arr+3  arr+4
```

对应关系：

* `arr[0]` == `*(arr + 0)` == `10`
* `arr[1]` == `*(arr + 1)` == `20`
* `arr[2]` == `*(arr + 2)` == `30`

---

## 14. 规范写法建议（提升代码可读性）✅

### 打印函数建议加 `const`

因为打印函数不需要修改数组，建议写成：

```cpp
void printArray(const int *arr, int len)
```

这样可以避免误改数据，也能表达“只读”的语义。

---

## 15. 一句话总总结（背这个就够了）🎯

**数组传参时会退化为首元素地址，所以函数参数用 `int *arr` 接收；而 `arr[j]` 本质等价于 `*(arr + j)`，`[]` 已经隐式完成了解引用，因此即使不显式写 `*`，也能访问并修改地址中的数据。**
